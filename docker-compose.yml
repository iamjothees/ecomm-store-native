services:
  app:
    container_name: ecomm-store
    image: ecomm-store-native:latest
    volumes:
      - ./src:/usr/src/app
    ports:
      - 5044:5173
      - 5045:4173
    environment:
      - CHOKIDAR_USEPOLLING=true